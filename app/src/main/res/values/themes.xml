<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PizzaParty" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->

        <!-- Added primary color styling for radio buttons (blue) -->
        <!-- Added text color styling (black) -->
        <!-- Added fullscreen functionality, set to true -->
        <item name="colorPrimary">@color/blue</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.PizzaParty" parent="Base.Theme.PizzaParty" />
</resources>